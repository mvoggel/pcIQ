# Supabase
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
DATABASE_URL=postgresql://postgres:password@db.your-project.supabase.co:5432/postgres

# EDGAR (no auth needed — public API, but set a descriptive User-Agent per SEC policy)
EDGAR_USER_AGENT="pcIQ matthewvoggel@gmail.com"

# Anthropic (Claude API — for signal summarization in Phase 2)
ANTHROPIC_API_KEY=
